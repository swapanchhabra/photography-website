---
interface Props {
  title: string;
  subtitle?: string;
  image: string;
  cta?: { label: string; href: string };
  fullHeight?: boolean;
  overlay?: boolean;
}

const {
  title,
  subtitle,
  image,
  cta,
  fullHeight = false,
  overlay = true,
} = Astro.props;
---

<section
  data-hero
  class:list={[
    'relative flex items-center justify-center overflow-hidden',
    fullHeight ? 'min-h-screen' : 'min-h-[60vh] lg:min-h-[70vh]',
  ]}
  aria-label={title}
>
  <!-- Background Image -->
  <div
    class="absolute inset-0 bg-cover bg-center bg-no-repeat"
    style={`background-image: url('${image}')`}
    role="img"
    aria-label={`Hintergrundbild: ${title}`}
  >
    {overlay && <div class="absolute inset-0 bg-gradient-to-b from-black/30 via-black/20 to-black/50" aria-hidden="true"></div>}
  </div>

  <!-- Content -->
  <div class="relative z-10 text-center px-6 max-w-4xl mx-auto">
    <h1 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-bold text-white tracking-tight text-balance animate-fade-in-up">
      {title}
    </h1>
    {subtitle && (
      <p class="mt-6 text-base sm:text-lg lg:text-xl text-white/90 font-light max-w-2xl mx-auto animate-fade-in-up animate-delay-200">
        {subtitle}
      </p>
    )}
    {cta && (
      <div class="mt-10 animate-fade-in-up animate-delay-300">
        <a
          href={cta.href}
          class="inline-block px-8 py-4 bg-accent text-white text-sm font-semibold uppercase tracking-widest hover:bg-accent-hover transition-all duration-300 hover:scale-105 min-h-[44px]"
        >
          {cta.label}
        </a>
      </div>
    )}
  </div>

  <!-- Scroll indicator -->
  {fullHeight && (
    <div class="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce" aria-hidden="true">
      <svg class="w-6 h-6 text-white/60" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </div>
  )}
</section>
